/*
This defines a custom TS type called student. A student object must have these 4 properties all of which must be strings.
*/

export type Student = {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
}